{
  "devDependencies": {
    "vitepress": "^1.0.0-beta.5"
  },
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build --max_old_space_size=4096 build",
    "preview": "vitepress preview"
  }
}
